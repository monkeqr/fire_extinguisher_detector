# 🧯 YOLOv11 Обнаружение Огнетушителей

## 🚀 Обзор Проекта

Этот проект реализует систему обнаружения объектов (Object Detection) для поиска огнетушителей, используя современную архитектуру **YOLOv11** от Ultralytics.

Проект структурирован в четыре основных этапа:
1.  **Тюнинг гиперпараметров (`finder_best_params.py`)**: Автоматизированный поиск оптимальных настроек для ускорения сходимости и улучшения точности.
2.  **Финальное обучение (`train.py`)**: Обучение модели на полном наборе данных с использованием лучших найденных гиперпараметров.
3.  **Тестирование на изображениях (`test_images.py`)**: Проверка качества детекции на статичных изображениях.
4.  **Тестирование на видео (`test_video.py`)**: Анализ производительности модели (FPS) и детекции в реальном времени на видеопотоке.

Модели обучены с использованием **Apple Silicon (MPS)** для максимальной производительности на устройствах Mac.

## 📋 Требования

Для запуска этого проекта вам понадобится:

* **Python 3.8+**
* **PyTorch**
* **Ultralytics YOLO** (самая свежая версия, поддерживающая YOLOv11)
* **PyYAML**
* **OpenCV** (`cv2` требуется для скрипта `test_video.py`)

Установка основных зависимостей:
```bash
python -m venv venv
```

```bash
source venv/bin/activate     # mac/linux
source venv/Scripts/activate # winows
```

```bash
pip install ultralytics torch torchvision torchaudio pyyaml opencv-python
```

Веса https://disk.yandex.ru/d/bq4CKEIg1B-meA
train/box_loss,train/cls_loss,train/dfl_loss,metrics/precision(B),metrics/recall(B),metrics/mAP50(B),metrics/mAP50-95(B),val/box_loss,val/cls_loss,val/dfl_loss,lr/pg0,lr/pg1,lr/pg2
0.67989,0.51325,1.09172,0.92014,0.82013,0.91292,0.73848,0.74984,0.57021,1.13648,0.0015842,0.0015842,0.0015842
